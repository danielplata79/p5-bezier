var inc = 0;

function setup() {
    createCanvas(windowWidth, windowHeight, WEBGL);
    widoth = windowWidth;
}

function draw() {
    background(0);

    rotate(60);

    for(var i = 0; i < 20; i++) {
        beginShape();
        for(var j = 0; j < 360, j+= 20) {
            
        }
        endShape();
    }
}